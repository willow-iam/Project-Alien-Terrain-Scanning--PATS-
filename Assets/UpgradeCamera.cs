using UnityEngine;
using System.Collections;

public class UpgradeCamera : MonoBehaviour {
	void Update () {
        /*if(Input.GetAxis("Mouse ScrollWheel")<0){
            transform.Translate(0,-2,0);
        }
        else if(Input.GetAxis("Mouse ScrollWheel")>0&&transform.localPosition.y<0){
            transform.Translate(0,2,0);
        }*/
	}
}
